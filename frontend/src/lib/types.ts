export type ToastDetails = {
  title: string;
  description: string;
};

export type PantryItem = {
  barcode: string;
  product_name: string;
  ingredient_name: string;
  image_url: string;
  quantity: number;
};
