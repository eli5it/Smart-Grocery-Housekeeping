import { describe, it, expect } from "vitest";

// TODO Implement Frontend Tests

// A Test written to validate CI workflow
describe("TEST for CI", () => {
  it("Is a passing test", () => {
    expect(true).toBe(true);
  });
});
